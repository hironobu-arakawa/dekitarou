# デキタロウ 開発ガイド

仕様は `docs/specs.md`(+末尾の改訂記録)。仕様の範囲を勝手に広げない。迷ったら「作らない」。

## コマンド

- `npm run dev` — 開発サーバー
- `npm run build` — 静的ビルド。**コンテンツ変更後は必ず実行する**(スキーマ検証と固定見出しチェックが走る)

## 構造の決まり

- 案件カードは **12セクション固定順**。見出しは `src/content/cases/temp-humidity-monitor.md` を正とする
- 「## 責任分界図」「## 現場の管理点との擦り合わせ」の直後には、frontmatter から図・表が自動挿入される(`src/pages/cases/[slug].astro`)。見出しが欠けるとビルドエラー
- frontmatter スキーマは `src/content.config.ts`。責任者は `contractor` / `customer` / `licensed` / `shared` の4値のみ

## コンテンツ執筆の制約(specs.md §9 に加えて)

このリポジトリのコンテンツはAIが下書きする前提である。AI生成は放っておくと
**製品カタログ・ノウハウカタログ**(機種の列挙、スペック比較、手順の羅列)に寄る。
それはQiita/Zennに無数にあり、このサイトの価値ではない。次を守ること。

1. **カタログ化の禁止** — 製品名・機種名の列挙、スペック比較表、セットアップ手順の詳細を書かない。構成は「案A/B/C」の一般形で示し、判断基準(誰が直せるか)に紙幅を使う
2. **全セクションが「仕事として閉じる」視点を持つ** — 責任・合意・撤退・保守のどれにも触れないセクションは書き直す。技術の説明で埋めない
3. **現場の5つの管理点(安全・品質・生産・原価・環境)を必ず擦り合わせる** — 先方はこの軸で現場を管理している。frontmatter の `concerns` は5軸すべて必須。「特になし」で流さない
4. **電気・資格の話に寄せすぎない** — 資格・法令は管理点の一部であって主役ではない。安全=電気ではなく、品質・生産・原価・環境も同じ重さで扱う
5. **カットオーバーで終わらせない** — 普通のプロジェクトはカットオーバーが終わり。製造業はカットオーバー後に成果が出る。「納品完了」と「効果の確認」を分けて書く
6. **AI由来の数値・法令記述は断定しない** — 費用・工数は幅、法令は「可能性がある」まで。現地実測・有資格者・所轄官庁への確認に必ず委ねる
7. 実在の企業・工場・案件を特定できる記述は書かない(specs.md §9)
